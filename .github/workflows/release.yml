name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"

      - name: Build macOS zip
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ARCHIVE="vixygrey-dev-setup-macos-${VERSION}.zip"
          mkdir -p build/macos
          cp scripts/setup-dev-tools-mac.sh build/macos/
          cp docs/SHORTCUTS.md build/macos/
          cp README.md LICENSE build/macos/
          mkdir -p build/macos/assets/wallpapers
          cp assets/wallpapers/dracula-sakura.jpg build/macos/assets/wallpapers/
          cd build/macos && zip -r "../../${ARCHIVE}" . && cd ../..
          shasum -a 256 "${ARCHIVE}" > "${ARCHIVE}.sha256"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@5113cdc90fd4d541c801c55356214017bf5ae34b # v3
        with:
          generate_release_notes: true
          files: |
            vixygrey-dev-setup-macos-${{ steps.version.outputs.version }}.zip
            vixygrey-dev-setup-macos-${{ steps.version.outputs.version }}.zip.sha256
